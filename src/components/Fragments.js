const Fragments = () => {
    return (
        <>
            <h1>Hello</h1>
        <p>Welcome to React</p>
        </>
      );
}
 /*
 In react componennts if we return multiple JSX  we have to enclose it in outer container
 To prevent multiple duplicate division we can use JSX Fragments <> </>
  */
export default Fragments;